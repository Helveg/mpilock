name: Black

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Black Check
      uses: psf/black@stable
      with:
        src: 'mpilock'
